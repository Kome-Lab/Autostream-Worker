services:
  worker:
    image: worker:latest
    restart: unless-stopped
    env_file: .env
    environment:
      AUTOSTREAM_BIND_ADDR: 0.0.0.0:8080
    ports: !override
      - "127.0.0.1:8084:8080"
