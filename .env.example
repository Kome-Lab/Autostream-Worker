AUTOSTREAM_NODE_CONFIG=/etc/autostream-worker/config.yml
# Optional override for local/dev builds. Release and Docker builds embed this automatically.
# SERVICE_VERSION=dev
AUTOSTREAM_ENV=production
AUTOSTREAM_REQUIRE_CONTROL_PANEL_RUNTIME_CONFIG=true
# Optional absolute-path override. When omitted, Worker selects this exact Noto
# path. A missing or invalid selected file fails closed; there is no basic-font fallback.
AUTOSTREAM_SCENE_FONT_FILE=/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc
CONTROL_PANEL_HEARTBEAT_INTERVAL_SEC=30
# Panel-generated config.yml supplies the service identity, public endpoint,
# Control Panel URL, Node Runtime Token, stream ingest key, and selects
# listener.credential: node-listener.json. The root-owned credential supplies
# the local bind_address and config_revision.
# SERVICE_CONTROL_TOKEN_SHA256=<SHA256_OF_CONTROL_PANEL_TO_SERVICE_TOKEN>
ENCODER_RECORDER_TIMEOUT_SEC=5
ENCODER_RECORDER_RETRY_MAX=2
ENCODER_RECORDER_RETRY_BASE_DELAY_SEC=1

# Encoder/Recorder route and signed ingest token are supplied by Control Panel
# in each stream job. Do not put production worker-event tokens in env.
# Standard operation sends signals through Control Panel with the Node Runtime Token.
# The listener credential must use an unprivileged local port (1024-65535).
AUTOSTREAM_WORKER_PORT=8084
AUTOSTREAM_WORKER_CONTAINER_PORT=8080
TZ=Asia/Tokyo
